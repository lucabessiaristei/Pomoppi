# Pomoppi Windows port — plan and status

This file is a self-contained handoff for a fresh session picking up the Windows port with no other context. Read this first, then `CLAUDE.md` for the general repo map, then `SPEC.md` for behavior.

**Status as of 2026-09-18: Phase W7 is done. Windows-port work is now content-complete except Obsidian (deliberately, indefinitely deferred) and W8 (packaging)/W9 (final docs pass), which are next.**

## Why this file exists

Porting Pomoppi (currently Swift/AppKit+SwiftUI, macOS-only) to also run on Windows, sharing as much code as possible. The plan and the VM environment took a very long single session to nail down — this file exists so that work isn't repeated. Everything below was decided or discovered firsthand; nothing here is speculative.

## Locked decisions — do not re-derive or re-litigate these

- **Swift everywhere.** `Sources/PomoppiCore` and `Sources/PomoppiSprites` are already 100%-portable (`Foundation`-only, verified by reading every file). `Sources/PomoppiRender` has exactly one Apple-only exit point: `PixelCanvas.makeImage() -> CGImage?`. Internally, `PixelCanvas` already manipulates a raw RGBA byte buffer directly (bypassing CoreGraphics's color-managed fill on purpose, for byte-exact color output) — the buffer itself has no platform dependency. The plan: strip `CGContext` from `PixelCanvas`'s internal storage entirely (plain byte buffer, `bytesPerRow = width * 4`), and add two small platform adapter files: `PixelCanvas+CoreGraphics.swift` (macOS, `#if canImport(CoreGraphics)`) and `PixelCanvas+GDI.swift` (Windows, `#if os(Windows)`). This is Phase W2.
- **Windows UI = direct Win32** via `import WinSDK` (User32/GDI32/Shell32/ComCtl32/AdvAPI32) — not WinUI3, not .NET/MSIX, not the `compnerd/swift-win32` framework (its own README says core interfaces are still unimplemented and stub out at runtime). New Windows-only executable target at `Sources/PomoppiWindows/`, mirroring `Sources/PomoppiApp/`'s responsibilities file-by-file (window, tray, hotkeys, login item, settings window — see the file map in the phase list below).
- **`Sources/PomoppiApp/` (the Mac shell, including the SwiftUI Settings window) is off-limits for the port itself.** This was the user's hard constraint going in. The **one** explicit exception: a new "reverse tray click" setting is being added to Settings on *both* platforms (see Phase W2b) — that's a deliberate new feature requested mid-planning, not scope creep.
- **Dev/test loop = a local Windows 11 ARM64 VM in UTM**, not Wine, not CI. Swift cannot cross-compile from macOS to Windows today (verified: no macOS-host Windows Swift SDK artifactbundle exists as of Sept 2026). The VM uses UTM's **Apple Virtualization backend** (not QEMU — native-speed, not emulated). Real Windows semantics, no CI/Wine round-trip needed for iteration. **The Windows app can only be built/tested inside this VM — never on the macOS host.** `swift build` on the Mac never compiles `Sources/PomoppiWindows/`.
- **CI is descoped to release-only, deferred to Phase W8** — not a per-commit gate, not the dev loop. The VM is ARM64-only and can't produce/test an x86-64 binary (what most real Windows machines are), so GitHub Actions (`windows-latest`, x64) stays solely for producing release artifacts in W8. Note: `compnerd/gha-setup-swift@v3`'s tag resolution was reported broken as of Sept 2026 — pin a commit SHA if/when this is set up.
- **Tray clock**: Windows' notification area has no text slot next to the icon (unlike macOS's menu-bar text). Accepted gap — the icon still animates the same way, and the live `mm:ss` moves to a hover tooltip (`"Focus — 25:00"`) on Windows only.
- **Tray click mapping changed from the original SPEC.** Current SPEC'd behavior (left-click = menu, right-click = raise) is being **replaced by the standard convention on both macOS and Windows by default** (left-click = raise/primary action, right-click = context menu), with a new `reverseTrayClick: Bool` field in `PomoppiCore/Settings.swift` (default `false`) plus a Settings toggle on both platforms to swap it back. This is **Phase W2b**, and it is the *only* phase in the whole plan that touches `Sources/PomoppiApp/` (specifically `TrayController.swift`'s click handling, and a new section in `SettingsView.swift`'s Window tab) — isolated deliberately so the rest of the port's "Mac app is untouched" invariant stays auditable. Must land before W4 (Windows tray reads the same setting). Known consequence to flag when it ships: any existing `settings.json` on disk lacks the key, decodes to `false`, and tray click behavior silently flips on next launch — intended, but tell the user when this phase is about to run.
- **Settings window UI parity = identical information architecture** (same 6 tabs, same labels, same validation, same shared `SettingsStore`/`PomoppiCore.Settings`), **not** a pixel match — Win32 common controls cannot replicate SwiftUI's look, and that's fine. The widget itself, being hand-drawn pixel art via the shared `PixelCanvas`, *can* and should be pixel-identical across platforms.
- **Docs convention going forward**: `SPEC.md` should get a `[macOS]` / `[windows]` / `[divergent]` / `[legacy]` tag per section (untagged = `[both]`, identical contract on both platforms) plus a `§0b Parity ledger` table (seed it with: tray click mapping, tray clock, settings chrome, shortcut display text, storage path, launch-at-login mechanism — one row each, macOS vs Windows). `CLAUDE.md`'s file map should split into Shared / macOS / Windows tables. This documentation work is **Phase W1** (scaffold) and **Phase W9** (final pass) — it has not been done yet as of this writing; SPEC.md and CLAUDE.md are still macOS-only documents.

## Full phase plan

Each phase should get the user's explicit go-ahead before starting the next — this project is worked one chunk at a time by preference, not all at once. Execution work (once a phase's approach is settled) should go to `dipsy` (general implementation) or `po` (small tasks) rather than being done all on the main thread, unless the user says otherwise.

- **W0 — VM feasibility spike. ✅ DONE.** See "Phase W0 outcome" below for the full story, gotchas, and current VM access details.
- **W1 — `Package.swift` restructure + empty Windows target + doc scaffold. ✅ DONE (2026-09-17).** See "Phase W1 outcome" below for the full story, the one new WinSDK gotcha found, and an open `FileManager.replaceItemAt` crash that needs a decision before it's safe to build on `PomoppiCore`'s Windows behavior further.
- **W2 — `PixelCanvas`/`WidgetRenderer` platform-seam refactor. ✅ DONE (2026-09-17), commit `74cb055`.** `PixelCanvas` now owns a plain `[UInt8]` byte buffer (`internal`, not `private` — the CoreGraphics adapter is a different file in the same module). `makeImage()` moved to new `Sources/PomoppiRender/PixelCanvas+CoreGraphics.swift`, guarded `#if canImport(CoreGraphics)`. `WidgetRenderer.draw()` keeps its public `CGImage?` signature unchanged; a new **internal** `WidgetRenderer.drawCanvas(...) -> PixelCanvas` lets tests assert on raw pixels via `canvas.pixel(x:y:)` without touching production call sites. `Sources/PomoppiApp/WidgetPixelView.swift`/`TrayController.swift` needed zero changes, confirmed. Verified via a throwaway FNV-1a checksum comparison (Swift's `Hasher` is randomly seeded per process, not suitable) of `WidgetRenderer.draw()`'s pixel output across 3 representative states (defaults/ringing/custom friend+background+frameStyle) before vs. after — byte-for-byte identical each time — plus full `swift test` (23/11/17 across the three targets) and a clean `swift run PomoppiApp` startup. `PixelCanvasTests.swift` needed zero changes (it already only used `pixel(x:y:)`).
- **W2b — Tray click convention + `reverseTrayClick` setting (macOS). ✅ DONE (2026-09-17), commit `ddce0bc`.** `reverseTrayClick: Bool = false` added to `PomoppiCore/Settings.swift` as a defaulted trailing init parameter (`CodingKeys`/`init(from:)`/`defaults` all updated, same pattern as every other stored Bool setting). `TrayController.handleClick()` re-reads it at click time (same "don't capture" pattern as `handleToggleAlwaysOnTop`): default is left=raise/right=menu, `reverseTrayClick=true` restores the original left=menu/right=raise. Toggle added to `SettingsView.swift`'s Window tab with a live-description footer. `SPEC.md`'s §9 tray section and the §0b parity ledger row updated. New `SettingsTests` case covers the default and the JSON-missing-key fallback. This was the only phase touching `Sources/PomoppiApp/` (just `TrayController.swift` + `SettingsView.swift`, confirmed nothing else there changed) — user was told about the settings.json migration behavior (existing files lack the key, decode to `false`, mapping flips on next launch) before this shipped.
- **W3 — Windows widget window.** Split into two verified sub-steps given its size.
  - **W3 part 1 — rendering adapter + storage path. ✅ DONE (2026-09-17), commit `27f77ed`.** `Package.swift`'s Windows branch now includes `PomoppiRender` (the first shared rendering code buildable on Windows) and `PomoppiWindows` depends on it. Required an unplanned but necessary follow-on fix: `WidgetRenderer.swift` had an unconditional top-level `import CoreGraphics` and public `draw() -> CGImage?` — guarded both `#if canImport(CoreGraphics)`, the same treatment W2 gave `PixelCanvas` (the file also defines `WidgetAnimationSnapshot`, needed by the otherwise-portable `WidgetAnimationController`, so the whole file has to stay buildable on Windows even though only `draw()` is Apple-only; `drawCanvas() -> PixelCanvas` remains the portable entry point). New `Sources/PomoppiRender/PixelCanvas+GDI.swift` (`#if os(Windows)`, mirrors `PixelCanvas+CoreGraphics.swift`): `makeLayeredBitmap(scale:) -> (hdc: HDC, bitmap: HBITMAP, previousBitmap: HGDIOBJ?, size: SIZE)?` builds a top-down 32bpp `CreateDIBSection` selected into a memory DC, integer-nearest-neighbor upscaled, RGBA swapped to GDI's native BGRA, alpha copied straight through un-premultiplied (safe since every pixel this app draws is alpha 0 or 255, where premultiplication is a no-op) — verified for real in the VM against a known test canvas (dimensions, R/B swizzle, alpha-0 passthrough, all at the right upscaled positions, all passed). New `Sources/PomoppiWindows/AppStorage.swift`: `storageDir() -> URL` via `SHGetKnownFolderPath(FOLDERID_RoamingAppData)`, mirroring `AppDelegate.storageDir()` — not wired into `main.swift` yet, that's part 2. `PomoppiRenderTests` deliberately stays off the Windows manifest for now: one test (`testRendersAtCanvasDimensions`) still reads `CGImage.width`/`.height` directly, unguarded — needs splitting/guarding before that test target can build on Windows (not done, not blocking).
  - **W3 part 2 — the actual window. ✅ DONE (2026-09-17), commit `ee200d1`.** `WidgetWindow.swift` creates the real layered popup (`WS_POPUP | WS_EX_LAYERED | WS_EX_TOOLWINDOW`, no `WS_EX_NOACTIVATE`), idempotent always-on-top (`SetWindowPos(HWND_TOPMOST/HWND_NOTOPMOST)` — those two constants don't import as usable symbols, reconstruct via `HWND(bitPattern: -1)`/`HWND(bitPattern: -2)`, new gotcha), and a ~60fps `SetTimer` loop that ticks the shared timer/animation, renders via `WidgetRenderer.drawCanvas` (widened to `public`), exports through `PixelCanvas+GDI`'s `makeLayeredBitmap`, and pushes via `UpdateLayeredWindow` — every GDI object disposed every tick (`defer` block) to avoid leaking a handle every ~16ms, confirmed flat handle count over ~50s/thousands of frames in the VM. `WidgetInput.swift` ports `WidgetPixelView`'s hit-testing (exact priority order), the <3px drag-vs-click threshold, and the full key table onto Win32's `WndProc`. `main.swift` does the real startup wiring now, replacing the W1 placeholder. Verified with a real screenshot (via the Task Scheduler interactive-session trick) plus a pixel-level transparency check at the widget's edges against the desktop background. **Deliberate scope cuts, not silently dropped:** no fade animation on show/hide (plain `ShowWindow`), settings button/key is a no-op (no settings window until W6/W7), Escape hides with no tray yet to un-hide from (until W4), and **the play button starts directly with no task-name prompt** (`StartCoordinator`'s prompt is AppKit/`NSAlert`-only) — this last one is a real, undecided behavior gap versus macOS, worth a decision before something later depends on task-name semantics (e.g. W5's shortcut table, or whenever Windows parity for `askForTaskName` gets addressed — no phase currently owns this explicitly).
- **W4 — Windows tray icon + menu. ✅ DONE (2026-09-17), commit `173b7a2`.** `Sources/PomoppiWindows/TrayController.swift`: `Shell_NotifyIcon` setup with a custom `WM_APP+1` callback message, tries `NOTIFYICON_VERSION_4` via `NIM_SETVERSION` with a graceful fallback to legacy v0 (both callback encodings recovered the same way — reading the low word of `lParam` works regardless of which version actually negotiated, no branch needed), 500ms icon/tooltip refresh with the same "only push what changed" discipline as macOS, `reverseTrayClick`-aware click dispatch, and a freshly-rebuilt-per-open context menu reusing `WidgetInput.swift`'s existing `activateButton()`/`toggleAlwaysOnTop()` rather than duplicating logic. New `Sources/PomoppiRender/PixelCanvas+GDIIcon.swift`: `makeIcon() -> HICON?` via `CreateIconIndirect` (alpha-carrying 32bpp color bitmap + throwaway mask, both disposed immediately since `CreateIconIndirect` copies and owns nothing). `WidgetWindow.swift`'s `WM_TIMER` case had a real latent bug fixed here: it previously assumed every timer tick was its own ~60fps frame loop, which broke once the tray's 500ms refresh timer started sharing the same `hwnd`/`WndProc` — now dispatches by timer ID. `PomoppiSprites` added as a direct `Package.swift` dependency of `PomoppiWindows` (needed for `GeneratedSprites.trayFrames` — a first-draft facade that routed around this via `PomoppiRender` was simplified away in favor of just adding the dependency). Shortcut-hint text in the menu deliberately omitted (needs Phase W5's `Shortcuts.displayWindows()`). Verified live in the VM: a real screenshot shows the tray icon rendering correctly and the actual context menu open with correct structure/greying/checkmarks, a synthesized click exercised `WM_COMMAND` end-to-end, and the icon is fully removed on quit (no ghost icon). **Reminder confirmed for real: `Shell_NotifyIcon` does not work from a plain SSH session** (Session 0 isolation) — the Task Scheduler `/it` trick from "Phase W0 outcome" below was required for every interactive check.

**Follow-up fix (2026-09-17), commit `be09ebd`, not part of a numbered phase**: the tray icon was hardcoded black, invisible on a dark taskbar. Win32 has no `NSImage.isTemplate`-style auto-tinting, so `TrayController` now reads `SystemUsesLightTheme` from `HKCU\...\Themes\Personalize` (re-checked every 500ms alongside the existing refresh) and draws black on light / white on dark. Verified against `reg query`'s own output.
- **W5 — Global hotkeys, login item, `Shortcuts.displayWindows`. ✅ DONE (2026-09-17), commit `84929b6`.** New `Sources/PomoppiWindows/GlobalShortcutManager.swift` (`RegisterHotKey`/`WM_HOTKEY`/`MOD_NOREPEAT`, tied to the widget's own hwnd rather than process-wide like Carbon). `Command`/`CommandOrControl`/`Control` all collapse onto `MOD_CONTROL` as planned — no real-world collision hit since every default accelerator is Alt+Shift+`<key>`. New `Sources/PomoppiWindows/LoginItem.swift` via `HKCU\Software\Microsoft\Windows\CurrentVersion\Run`. `Shortcuts.displayWindows(_:)` added, trivial (`accel.isEmpty ? "Not set" : accel"` — the normalized form is already Windows' plain-text shape). All 6 hotkeys + the login-item registry round-trip verified live in the VM. **Task-name prompt explicitly NOT built this phase** — despite the phase's own title, the plan's actual bullet body never described a mechanism for it, and a native Win32 modal dialog is a separate, sizable UI task on the order of W6/W7's settings window; `startPause` starts directly with no prompt, same known gap flagged since W3.
- **W6/W7 — Settings window, split in two (it's the largest chunk).**
  - **W6 part 1 — shell. ✅ DONE (2026-09-17), commit `73c6a89`.** New `Sources/PomoppiWindows/SettingsWindow.swift`: a real titled top-level window (not layered, unlike `WidgetWindow`) with a `SysTabControl32` hosting all 6 tabs in the correct order, one placeholder page each. Singleton `show(settingsStore:)` is the sole entry point (creates once, brings the existing instance forward on later calls); `WM_CLOSE` only destroys the settings window. All three "open settings" triggers (widget gear/`,` key, tray menu, global hotkey) now funnel through `activateButton("settings") → WidgetWindow.onOpenSettingsRequested → SettingsWindow.show()`. **Real, subtle bug found and fixed during verification**: all 6 pages share the exact same rect (only one shown at a time); `WS_CLIPSIBLINGS` on that page style made Windows clip each page's paint region against its 5 fully-overlapping siblings *regardless of whether those siblings were actually visible* — window/control creation, message dispatch, and even UI Automation's own "is this on-screen" reporting all looked completely normal, but every GDI draw silently landed outside the resulting empty clip region. Removing `WS_CLIPSIBLINGS` (pages never have a visible overlapping sibling to clip against here) fixed it — a real gotcha worth remembering for any future overlapping-same-rect-children pattern in this app. Verified live: correct tab order/switching, singleton reuse (same HWND across two separate open-settings triggers), `WM_CLOSE` leaves the main widget process running.
  - **W6 part 2 — real Rhythm/Window/Sound tab content + the `reverseTrayClick` toggle. ✅ DONE (2026-09-17), commit `d6061fd`.** 4 numeric steppers + 3 checkboxes on Rhythm, 5 checkboxes on Window (`reverseTrayClick` included), 1 checkbox + 1 stepper on Sound — `msctls_updown32` + buddy-`Edit` for steppers, `ringSeconds`' step-of-5 done by intercepting `UDN_DELTAPOS` and scaling the delta before `UDM_SETPOS32`. **Real routing bug found and fixed**: controls are children of their tab's page, not the top-level window, and Win32 delivers `WM_COMMAND`/`WM_NOTIFY` only to the immediate parent — the page's `WndProc` was still the bare `DefWindowProcW` from part 1 (fine when a page only held one inert placeholder), so nothing ever reached `SettingsWindow`'s dispatch. Fixed by giving the page class its own `WndProc` forwarding just those two message types to `GetParent`. Verified live, patiently (one action → wait → confirm → next): every persistence path, the step-5 behavior, typed-value commit, and `soundEnabled`'s live enable/disable of `ringSeconds` all confirmed via direct `settings.json` reads. One apparent bug (a 4th stepper not rendering) turned out to be the always-on-top widget window physically overlapping the settings dialog during testing — not a defect, just two independently-correct always-on-top windows sharing screen space at their default positions.
  - **W7 — Appearance + Keys only, Obsidian deliberately deferred.**
    - **Keys tab (shortcut recorder). ✅ DONE (2026-09-18), commit `40770fe`.** One row per `Shortcuts.action` with a click-to-record button (`Shortcuts.displayWindows`), bare Escape cancels, Reset to Defaults, a static info list of fixed widget keys. Recording temporarily unregisters every live global hotkey so a combo can be rebound to itself. **Real bugs found and fixed**: abandoning a recording via tab-switch or closing the settings window left every hotkey permanently dead (only an actual capture called the re-registration path — fixed with a `stopRecording()` guard on both `selectTab` and `WM_CLOSE`); `registerGlobalShortcuts`'s own "skip if unchanged" memoization would skip re-applying after a no-net-change recording, even though everything had just been unregistered (fixed with `reapplyGlobalShortcuts()` in `main.swift`, which invalidates the memo first). Keyboard-capture routing was new territory: `WM_KEYDOWN` goes to whichever HWND has focus directly (unlike `WM_COMMAND`/`WM_NOTIFY`, which already bubble via W6's page-forwarding fix) — `startRecording` explicitly moves focus to the Keys page so the capture keystroke lands somewhere real, `stopRecording` moves it back afterward or the page would silently swallow every future keydown. Also fixed a regression this phase caught: the grid/luna background swap (`b8edbc3`) broke `GeneratedSpritesTests` (still asserted the old scacchi roster) — missed at the time because verification only checked the tail of `swift test`'s output instead of every target's actual pass/fail line; lesson recorded in the `feedback-dont-overtest`-adjacent verification discipline.
    - **Appearance tab, part 1 of 2 — picker grids. ✅ DONE (2026-09-18), commit `d5f8e28`.** Friend/window-edge/background pickers as `BS_OWNERDRAW` buttons, drawn via a new `Sources/PomoppiWindows/AppearancePreviews.swift` (mirrors `PixelPreviews.swift`'s crop/tint logic fresh rather than sharing it — `Sources/PomoppiApp/` stays untouched) and a new `PixelCanvas.draw(into:destRect:...)` GDI blit helper (`StretchBlt` + `COLORONCOLOR`, same hard-edge look as every other blit). `WM_DRAWITEM` now forwards through the settings page `WndProc` alongside `WM_COMMAND`/`WM_NOTIFY` (same "immediate parent only" rule). A card's click reuses the existing `pushButtons`/`BN_CLICKED` dispatch; selecting one repaints every card so the moved border shows immediately, and ink/paper/frameStyle are read fresh from `settingsStore` on every repaint (not cached at button-creation time) so a later color/theme change will recolor correctly without this code changing. **New tooling gotcha, not an app bug**: PowerShell does not marshal `$null` to a true native NULL for a P/Invoke `string` parameter — it coerces to `""`, which then requires an exact empty-string match — so `FindWindow` calls from a verification script must pass the *real* class **and** title together rather than leaving either as `$null`. Verified live in the VM: all three grids render with correct default selections (Namidappi/Scallopy/Grid), and clicking an unselected card moves the border and persists to `settings.json` (`friend` → `"onanippi"`, confirmed by reading the file back) — one grid verified end-to-end per `feedback-dont-overtest`, not all three separately, since they share the exact same code path. Next: **part 2** — theme-preset grid, ink/paper `ChooseColorW` pickers, the segmented scale picker (1×-4×), and the `Trackbar32` opacity slider. See `Sources/PomoppiApp/SettingsView.swift`'s `AppearanceTab`/`ThemePresetPicker` for the exact macOS reference.
    - **Appearance tab, part 2 of 2 — theme/color/scale/opacity. ✅ DONE (2026-09-18), commit `aad1fa4`.** A 12-preset theme grid (plain paper-fill + ink-dot swatches, no PixelCanvas — there's no art to preview, just the two colors), ink/paper pickers via `ChooseColorW` (`lpCustColors` kept at instance scope so a custom color persists across repeat opens), 4 plain buttons standing in for macOS's segmented scale picker, and an `msctls_trackbar32` opacity slider (needs `ICC_BAR_CLASSES`) with a live "NN%" readout via `WM_HSCROLL`. This pushed total content well past the fixed window's height, making Appearance the only page with its own `WS_VSCROLL` and a real scrollbar. **First attempt used `ScrollWindowEx`'s `SW_SCROLLCHILDREN` and it visibly corrupted the page** — confirmed via a real screenshot showing ghosted stale fragments of moved controls after scrolling — a real, MSDN-documented limitation of that flag with children straddling the scroll boundary, not a fluke or a one-off race (a follow-up `RedrawWindow(...RDW_UPDATENOW...)` didn't fix it either). Fixed by abandoning `ScrollWindowEx` entirely: every Appearance-page control now records its own un-scrolled base position at creation time (`AppearanceControlPosition`), and scrolling repositions all of them directly via `SetWindowPos` + one synchronous `RedrawWindow` — no blit-shortcut anywhere, so nothing to ghost. **Second real bug found and fixed**: `STATIC` label text treats a bare `&` as an Alt-mnemonic marker by default (consumed, next character underlined) — "Size & transparency" rendered as "Size_transparency" until `addLabel` started passing `SS_NOPREFIX` unconditionally. Verified live in the VM: all sections render correctly (colors/selection state matching current settings, including the widget's own live preview), scrolling is clean with no artifacts, a scale-button click persists to `settings.json` and immediately resizes the live widget, and `ChooseColorW` opens with the current color pre-selected and Cancel leaves settings untouched. **Windows-port work is now content-complete except Obsidian** (deliberately, indefinitely deferred) and W8/W9.
    - Both driven by the same shared `SettingsStore` — validation is not reimplemented. **The Obsidian tab is skipped indefinitely** — the user wants to redesign session logging entirely (both platforms) into a plain JSON internal format that's exportable to different editors, not Obsidian-markdown-specific like today's `ObsidianLogger.swift` — see the `project-obsidian-logging-redesign` memory. Don't build a Windows Obsidian tab or touch `ObsidianLogger.swift` until that redesign happens; the Obsidian page stays the existing placeholder.
- **W8 — Packaging. ✅ DONE (2026-09-18), commit `5c4b942`.** `Scripts/make-windows-app.js`: a release build (`-Xlinker /SUBSYSTEM:WINDOWS -Xlinker /ENTRY:mainCRTStartup` for a console-free GUI app off a plain `main.swift`, no `WinMain` needed), MSVC located dynamically via `vswhere.exe` rather than a hardcoded path (this VM's Build Tools install and GitHub's hosted Enterprise install use different roots — confirmed by testing both), `dist/Pomoppi-win/` assembled (exe renamed to `Pomoppi.exe`, the long-dormant `Pomoppi.exe.manifest` copied alongside it as an external side-by-side manifest — finally wired in, exactly as CLAUDE.md's invariant said it would be — every Swift runtime DLL from the toolchain's `Runtimes` dir, an icon if `assets/pomoppi.ico` exists yet — it doesn't, a separate unplanned task, logged rather than failing the build), then zipped. `.github/workflows/windows.yml` is release-only (`workflow_dispatch` + `release: types: [published]`, not a per-commit gate), `compnerd/gha-setup-swift` pinned to commit `397094e75494a93fa8d81db0268dbc8f5d6cf7c6` (that repo's real latest commit as of today, not empirically tested — see the workflow's own header comment) per the tag-resolution-broken note above. Drafted by the `po` subagent, then verified for real end-to-end on this project's dev VM: needed Node.js installed first (wasn't there — `winget install --id OpenJS.NodeJS.LTS`), then two real bugs surfaced and got fixed — `vswhere`'s default query silently excludes a Build-Tools-only install unless you pass `-products *` (works fine without it against a full VS install, i.e. would have passed on GitHub's runner and quietly failed only here), and Node's `execFileSync('cmd.exe', ['/c', cmdStringWithItsOwnQuotes])` re-escapes those already-embedded quotes into literal `\"` that `cmd.exe` can't parse at all unless `windowsVerbatimArguments: true` is set. Confirmed working: a real release build, correct `dist/` contents (35 DLLs), a produced zip, and the packaged `Pomoppi.exe` launches and creates its widget window. **The GitHub Actions workflow itself has never actually run** — nothing in this phase pushed anything or triggered CI; that's the one piece someone still needs to do once, for real, before trusting it.
- **W9 — Final docs pass.** Fill in every SPEC.md platform tag and the parity ledger with what W1–W8 actually produced; finalize CLAUDE.md's three-way file map (Shared / macOS / Windows) and invariants.

## Phase W0 outcome — the VM environment (done, 2026-09-17)

### Access

- VM name in UTM: `pomoppi-windows` (Apple Virtualization backend, ARM64, 6GB RAM, 4 CPU cores, 80GB disk, Windows 11 Pro 25H2 build 10.0.26200.8037).
- SSH: `ssh pomoppi-win` — this alias is already in `~/.ssh/config` on the Mac (host `192.168.64.2`, user `bubvm`, key `~/.ssh/pomoppi_win_vm`, a dedicated throwaway keypair). Should still work as long as the VM exists and the Mac's `~/.ssh/config`/key files aren't touched.
- Windows account: `bubvm`, password `<redacted — ask for the current VM password>`, local admin (chosen interactively during install, not scripted).
- Toolchain already installed *inside* the VM: Visual Studio Build Tools 2022 (17.14.41) with the C++ workload + ARM64 targeting (`MSVC 14.44.35207`, confirmed native `cl.exe`/`link.exe` in `bin\Hostarm64\arm64`) + Windows 11 SDK; Swift 6.4 (`aarch64-unknown-windows-msvc`). To get the MSVC environment into a shell: `call "C:\Program Files (x86)\Microsoft Visual Studio\2022\BuildTools\VC\Auxiliary\Build\vcvarsall.bat" arm64` before `swift build`.
- Edit-on-Mac loop: edit locally, `scp` the changed files to the VM (or `git clone`/`git pull` inside the VM if preferred going forward), then `ssh pomoppi-win` to build. This was the loop used for the W0 spike and works fine.

### Everything learned the hard way (read before touching the VM setup again)

- **UTM's AppleScript `make new virtual machine` cannot reliably attach ISO drives.** It has real, confirmed gaps for the Apple Virtualization backend: no display device unless you explicitly pass `displays:{{dynamic resolution:true}}` (otherwise eternal black screen, no error); its `OperatingSystem` enum has no "Windows" case (`none`/`linux`/`macOS` only — "Linux" + UEFI boot *is* the correct generic-EFI config Windows uses too, a GUI label reading "Machine: Linux" is expected, not a bug); and a sandboxed UTM process cannot open an externally-referenced ISO attached via AppleScript at VM-start — fails with "Cannot access resource" regardless of which directory the file is in or its quarantine flag. **The VM that's currently running was created through UTM's own GUI wizard** (Virtualize → Windows → browse to the ISO inside the wizard's own install step) after AppleScript-based creation proved unfixable.
- The wizard can still silently produce a QEMU backend VM instead of Apple Virtualization depending on exact clicks. If ever recreating this VM, verify via `osascript -e 'tell application "UTM" to configuration of (virtual machine named "pomoppi-windows")'` — QEMU-only fields (`hypervisor`, `machine`, `uefi`, `qemuAdditionalArguments`, `directoryShareMode`) mean it's QEMU and needs recreating.
- UTM's Apple Virtualization VMs only reliably support **one** externally-attached removable drive — a second one (e.g. for an unattended-install answer file) causes an unfixable "storage device attachment is invalid." A fully unattended (`autounattend.xml`) install was attempted and abandoned for this reason; the current VM was installed **interactively**.
- Fresh VM firmware doesn't auto-pick a boot entry (drops to the UEFI Interactive Shell — fix: `FS0:`, `cd efi\boot`, `bootaa64.efi`, then press a key immediately when "Press any key to boot from CD or DVD" appears). The same problem recurs after Setup's first reboot as a native boot-picker UI that sometimes ignores all input — `utmctl stop --kill` then `utmctl start` reliably un-sticks it (`utmctl` is at `/Applications/UTM.app/Contents/MacOS/utmctl`).
- **UTM's "Install Windows Guest Tools…" package bundles a broken display/GPU driver for this exact Apple-Virtualization + Windows-ARM64 combination.** Installing it boots successfully but renders solid black forever after the resize. If this happens: Safe Mode (WinRE → Troubleshoot → Advanced options → Startup Settings → Restart → press 4/F4) → Device Manager → Display adapters → Uninstall device + delete driver software → reboot normally. The *network* driver alone is safe and was used successfully: Device Manager → the unknown "Ethernet Controller" under Other devices → Update driver → Browse → point at the mounted guest-tools disc root with "Include subfolders" checked (finds the `virtio-net-pci` driver without touching display).
- OOBE's mandatory "Let's connect you to a network" screen bypasses cleanly with no driver at all: Shift+F10 (hold **Fn** too on a Mac keyboard — bare F10 is the system mute key) → `start ms-cxh:localonly` → falls straight through to local account creation. Use this proactively rather than fighting the network driver during install.
- Enabling SSH needs an **elevated/Administrator** Command Prompt — `Add-WindowsCapability -Online -Name OpenSSH.Server~~~~0.0.1.0`, then `Start-Service sshd` + a firewall rule for port 22; a non-elevated prompt fails with "requires elevation."
- Admin-account pubkey auth needs the key in `C:\ProgramData\ssh\administrators_authorized_keys` (not the per-user `~/.ssh/authorized_keys`), with `icacls` restricted to Administrators+SYSTEM — this is a hard Windows OpenSSH requirement for admin accounts, not optional.
- **SSH sessions run outside the interactive desktop (Windows Session 0 isolation).** `Shell_NotifyIcon` and `RegisterHotKey` both fail (confirmed via a real spike — `E_FAIL` / error 1459) when called from a plain `ssh` session. Compiling and most testing works fine headlessly over SSH; anything touching the tray or global hotkeys (i.e. all of Phase W4/W5's actual functionality) needs to run in the logged-in interactive session instead. The fix used and confirmed working:
  ```
  schtasks /create /tn SomeTest /tr "C:\path\to.exe" /sc once /st 23:59 /it /ru bubvm /f
  schtasks /run /tn SomeTest
  ```
  `/it` runs it with the interactive token of the already-logged-in user (`bubvm`), no separate credentials needed. Since stdout isn't captured this way, have the program log results to a file (e.g. under `%APPDATA%`) and read that back over SSH afterward.
- The W0 spike itself (a throwaway `import WinSDK` package, not part of the real app) surfaced several real WinSDK/Swift-interop gotchas worth knowing before writing `Sources/PomoppiWindows/` for real: Win32 flag constants like `MOD_ALT`, `NIF_ICON`, `WM_APP` import as plain `Int32` in this Swift overlay, not as an `OptionSet` — don't write `.rawValue` on them, just cast directly (`UINT(MOD_ALT | MOD_SHIFT)`). `SHGetKnownFolderPath` wants an `UnsafePointer<KNOWNFOLDERID>`, and a bare `GUID` constant like `FOLDERID_RoamingAppData` won't implicitly convert — go through `withUnsafePointer(to:)` + `withMemoryRebound(to: KNOWNFOLDERID.self, capacity: 1)`. `IDI_APPLICATION` doesn't import as a usable symbol — use `UnsafePointer<WCHAR>(bitPattern: 32512)` (its raw resource ID) instead.

## Phase W1 outcome (done, 2026-09-17)

`Package.swift` now branches on `#if os(Windows)` at manifest-eval time: a Windows host sees only `PomoppiCore`/`PomoppiSprites`/the new `PomoppiWindows` executable (depends on `PomoppiCore` only, no `PomoppiRender` yet)/their two test targets; a non-Windows host sees the exact same target list as before (verified byte-identical, `swift build && swift test` still green on macOS, all pre-existing tests passing). `Sources/PomoppiWindows/main.swift` is a real, VM-verified Win32 skeleton (`RegisterClassW`/`CreateWindowExW`/`GetMessageW` loop/`WM_DESTROY`→`PostQuitMessage`), plus `Sources/PomoppiWindows/Pomoppi.exe.manifest` (Common Controls v6 + per-monitor-v2 DPI awareness, not wired into any build step until W8). `SPEC.md` got its `§0a` platform-scope-tag convention + a seeded `§0b` parity ledger (6 rows, macOS column filled from real current behavior, Windows column "not yet implemented"); existing sections are deliberately **not** retagged yet — that's progressive, finished in W9. `CLAUDE.md`'s file map is now three tables (Shared/macOS/Windows) with one new invariant about the Windows app only ever building/running inside the VM.

**Repo now lives on the VM too**, synced via `tar | ssh ... tar -x` (excluding `.git`/`.build`/`node_modules`/`.dev-app-support`) into `C:\Users\bubvm\pomoppi`. This tar-based sync is unproven for repeat use — next session should decide whether to keep re-tarring changed files or switch to a git-based sync (commit+push+`git pull` inside the VM) before Phase W2/W3 iteration ramps up.

**One new WinSDK/Swift interop gotcha** (add to the running list in "Everything learned the hard way" above): `GetMessageW`'s `BOOL` return imports as a plain Swift `Bool` in this overlay, not `Int32`/`WindowsBool` — `while GetMessageW(...) > 0` fails to type-check; use `while GetMessageW(&message, nil, 0, 0) { ... }` instead.

**`replaceItemAt` crash — FIXED (2026-09-17).** `FileManager.replaceItemAt` is not implemented in swift-corelibs-foundation on Windows; both call sites (`Settings.swift:331`, `ObsidianLogger.swift:74`) now branch on `#if os(Windows)`: Windows falls back to `removeItem` + `moveItem` (loses `replaceItemAt`'s atomicity/backup guarantees — an accepted trade-off, not worth hand-rolling something fancier), non-Windows keeps `replaceItemAt` exactly as before. Verified both ways: `swift test` green on macOS (23/23 `PomoppiCoreTests` incl. `ObsidianLoggerTests`) and, re-synced into the VM, green there too (23/23 `PomoppiCoreTests` + 11/11 `PomoppiSpritesTests`, `SettingsTests.testClampingRejectsOutOfRangeAndUnknownValues` and all `ObsidianLoggerTests` passing with no fatal error).

**VM sync strategy — superseded, now git-based (2026-09-17).** Initially decided to stick with tar/scp (see git history of this file for that reasoning), then the user overrode that and asked to use git anyway. Git is now installed on the VM (`git version 2.55.0.windows.3`, via `winget install --id Git.Git`) and `C:\Users\bubvm\pomoppi` is a real clone tracking the Mac's `main` branch history (bootstrapped via `git bundle`, since there's no GitHub remote — this is a fully local, offline sync with no server involved). Confirmed working both for the initial clone and a real incremental pull.

**Ongoing sync recipe for W2+:**
On the Mac (repo root): `git bundle create /tmp/pomoppi.bundle main` (or a narrower `<last-synced-sha>..main` range), then `scp /tmp/pomoppi.bundle pomoppi-win:'C:/Users/bubvm/pomoppi.bundle'`.
On the VM, inside the existing clone: `git pull C:\Users\bubvm\pomoppi.bundle main`.
No `git init`/reclone needed for subsequent syncs — that's a one-time bootstrap step already done. (One first-time-only quirk hit during bootstrap, not relevant again: a bundle created from a branch name rather than `HEAD` has no HEAD symref, so the very first `git clone` off a bundle needs a follow-up `git checkout -b main origin/main` — a plain `git pull <bundle> <branch>` into an already-cloned repo doesn't hit this.)

**Only committed history syncs this way — uncommitted Mac working-tree changes don't transfer to the VM.** This repo currently carries real uncommitted WIP unrelated to the Windows port (art-pipeline file renames, `CLAUDE.md`/`README.md` doc edits) that stays exactly where it is; only what actually gets committed on the Mac reaches the VM. Keep that in mind each phase: the VM build reflects the last *commit*, not the last edit.

## What to do next

W1–W8 are all done. What's left:

- **Actually run `.github/workflows/windows.yml` once, for real** (`workflow_dispatch` from the Actions tab, or publish a test release) — it's never been executed, only read carefully. Watch for the same two classes of thing every other phase has hit: an action input silently accepted but not doing what its name implies, or a path/tool assumption that holds on this dev VM but not on GitHub's runner image (Build-Tools-vs-Enterprise vswhere resolution was exactly this kind of gap, caught only by testing on the real VM instead of trusting the plan).
- **W9 — final docs pass**: fill in every `SPEC.md` platform tag and the `§0b` parity ledger with what W1–W8 actually produced; finalize `CLAUDE.md`'s three-way file map and invariants (the Windows-file-map row already reflects most of this as each phase landed, but hasn't had a dedicated finishing pass). Not urgent — the code and this plan doc are both already accurate; W9 is about polishing the *other* docs to match.
- **`assets/pomoppi.ico` doesn't exist** — `make-windows-app.js` ships without an icon until someone adds one. Not a phase, just a known, deliberately-logged gap.
- Obsidian stays deferred indefinitely (see `project-obsidian-logging-redesign`).
- Carried forward, still not blocking anything: `Shortcuts.displayWindows(_:)` (added in W5) would unblock adding shortcut-hint text to W4's tray menu, if that's ever worth doing.

Also carry forward: the task-name-prompt gap flagged in W3 part 2 — W5 is the natural place to actually resolve it, since it's the phase that would otherwise wire `startPause`'s shortcut straight to `timer.start()` the same way the tray/widget buttons currently do, same gap. VM sync note: after a `git pull <bundle> main` fails with local-changes-would-be-overwritten (happened a few times now, from dev-iteration files never committed/cleaned on the VM side), the fix is `git checkout -- . && git clean -fd` on the VM first, then retry — safe, the VM is a pure build/test mirror.
